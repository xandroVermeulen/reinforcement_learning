import { InjectElementDirective } from './inject-element.directive';

describe('InjectElementDirective', () => {
  it('should create an instance', () => {
    const directive = new InjectElementDirective();
    expect(directive).toBeTruthy();
  });
});
